import { BpricepipePipe } from './bpricepipe.pipe';

describe('BpricepipePipe', () => {
  it('create an instance', () => {
    const pipe = new BpricepipePipe();
    expect(pipe).toBeTruthy();
  });
});
